# Required: absolute or relative path to the official CSV source.
# Example for this classroom repo layout when the source lives outside Git:
# SOURCE_FILE_PATH=../../source_data/servidores-2026.csv
SOURCE_FILE_PATH=/absolute/path/to/servidores-2026.csv

# Optional: generated lake/semantic output root. Default: ./data
DATA_ROOT=./data
